import React, { memo } from "react";
import type { FC, ReactNode } from "react";

const Dashboard: FC = () => {
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  );
};

export default memo(Dashboard);
